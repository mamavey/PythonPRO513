# number = int("hello")  # Невозможно преобразовать строку в число >> ValueError: invalid literal for int() with base 10: 'hello'

# result = 10 / 0  # На ноль делить нельзя >> ZeroDivisionError: division by zero

# file = open("nonexistent_file.txt")  # Файл не найден FileNotFoundError: [Errno 2] No such file or directory: 'nonexistent_file.txt'

# my_list = [1, 2, 3]
# print(my_list[5])  # Индекс вне диапазона >> IndexError: list index out of range

# result = "text" + 5  # Нельзя складывать строку и число >> TypeError: can only concatenate str (not "int") to str
